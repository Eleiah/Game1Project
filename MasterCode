#include <Wire.h>

void setup() {
 
Wire.begin(); // join i2c bus (address optional for master)

}

void loop() {
  
 Wire.beginTransmission(8);
  Wire.write(x); // x is the value coresponding to the desired sound
  Wire.endTransmission();
  
  
}
